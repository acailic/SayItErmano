"""Committed synthetic evaluation corpus (plan P1.4).

CC0-1.0 by construction: the wavs contain synthesized tones/silence —
no recorded speech — and are regenerated from the per-case ``synth``
recipes by fluidvoice/evalharness/synth.py (nothing binary is committed;
generated files match the global ``*.wav`` gitignore).
"""
