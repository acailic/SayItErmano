Contextual formatting for chat apps + terminal safety - roadmap "Later" items "Slash-command/mention literal formatting + terminal autocomplete spacing" and the spoken-send terminal blocklist noted ⏳ in docs/UPSTREAM-TRACKING.md line 69. Upstream-parity work; every semantic below must be verified against the upstream Swift source (github.com/altic-dev/Fluid-oss, text-processing files) during planning, same as the punctuation port was (docs/BEHAVIOR-SPEC.md §2).

STATUS: SHIPPED

<!-- shipped in c42879b -->

Today: post_process (fluidvoice/processing/__init__.py) runs fillers -> dictionary -> punctuation -> extra_formats; there is no slash/mention handling, so dictating "/ fix the deploy" inserts "/ fix the deploy" and Slack needs the slash joined to fire the command. Spoken-send (daemon.py:149-157, parse_spoken_send) strips the phrase and presses Enter unconditionally - in a terminal that EXECUTES a half-typed shell line; upstream blocks Enter in a terminal blocklist.

Scope:
1) Slash/mention squeeze, new fluidvoice/processing/slash.py applied in post_process (verify upstream's position in the chain): a `/` or `@` at the START of the text or preceded by whitespace, followed by exactly one space and a word char, joins to the next token - `/ fix the deploy` -> `/fix the deploy`, `@ John Smith` -> `@John Smith` (first space only, never the rest of the phrase). MUST NOT touch: mid-token sigils (URLs `https://…`, emails `a@b`), sigils followed directly by text (already joined), a lone trailing `/` or `@`. Verify whether upstream gates this by target app or runs unconditionally (the punctuation port found the live behavior ungated - docs/BEHAVIOR-SPEC.md:153); if upstream gates it, gate on the same signal, else run unconditionally. Config: processing.slash_mention_squeeze = true default.
2) Terminal autocomplete spacing: when the insertion target's WM_CLASS (insertion.py active_window_class) matches the terminal list (3), typed insertions that end in a word character get one trailing space so terminal autocomplete commits (upstream behavior - verify exact rule). Config: insertion.terminal_autocomplete_space = true.
3) Terminal class list + spoken-send blocklist: config key terminal_apps (list of case-insensitive WM_CLASS substrings; default: gnome-terminal, kgx, konsole, xterm, alacritty, kitty, wezterm, ghostty, foot, tilix, terminator, guake, yakuake, st-256color). In those apps the spoken-send phrase strips from the text but Enter is NOT pressed - verify what upstream does instead (nothing vs newline) and match; the pill/notification already shows the send state, no new surface.
4) doctor: one resolution line for each new key; README config table row.

Where: fluidvoice/processing/slash.py (new), fluidvoice/processing/__init__.py (one call), fluidvoice/insertion.py (trailing-space helper keyed on terminal_apps), fluidvoice/daemon.py (blocklist branch next to parse_spoken_send), fluidvoice/config.py, tests/test_processing.py (squeeze table incl. the negative cases above), tests/test_insertion.py (terminal spacing + class matching), tests/test_config_settings.py, tests/test_infra.py (doctor lines). NOTE daemon.py and config.py may be mid-flight from the parakeet work - plan against the then-current HEAD.

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; unit tests for the squeeze table (positives + URL/email/prose negatives), terminal class matching, and the blocklist branch (Enter suppressed in terminal, pressed elsewhere); config tests for the three new keys; upstream semantics for each rule quoted in the spec with file:line from Fluid-oss.

Out of scope: Wayland, per-app prompt sets, GAAV, changing punctuation or filler behavior, the chat-store/command-mode tool schema.
