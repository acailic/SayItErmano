Command mode v2 - deepen the port's clearest differentiator: voice-run commands with confirm-first safety. Command mode v1 shipped (requests/command-mode.md): strict-JSON single-tool protocol, every command confirmed, pill overlay feedback. Upstream parity items still open in docs/UPSTREAM-TRACKING.md and ROADMAP "Later": "Command mode (voice -> terminal agent) with the upstream tool schema and destructive-command confirmation list" - the chat store and native tool_calls remain upstream-only. Sequence AFTER requests/history-integrity.md lands so usage stats can validate the investment with clean data.

STATUS: SHIPPED

<!-- shipped in 396e63e (series 03d243f..396e63e) -->

Today: fluidvoice/command.py runs one command per dictation - strict JSON single tool, confirm-every-run; history stores mode/command/purpose/exit_code/success/output rows (fluidvoice/history.py). There is no follow-up context (each voice run starts cold), no destructive-command classing beyond confirm-always, and no History-window surface for command rows - the History list shows them as text entries without retry/copy affordances.

Scope (verify every semantic against the upstream Swift source - github.com/altic-dev/Fluid-oss, command/chat files - during planning, quote file:line in the spec, same as the punctuation port):
1) Tool schema parity: multi-tool JSON (tools array) with per-tool args validation where upstream validates; port the upstream schema shape, diverging only where Linux needs (paths, shell). Destructive-command confirmation list: port upstream's classification list verbatim + a user-extensible config list (command_mode.destructive_patterns) - destructive commands get the strong-confirmation UX (distinct pill color + explicit "confirm destructive" step), non-destructive keep the light confirm.
2) Conversation store: follow-up context per app+session window - the last N command results (N small, 5) are passed as context to the model for the next voice command in the same focused app within a timeout (config command_mode.context_window_s = 300, 0 disables); "new session" spoken phrase or config clears it. Store in memory only - nothing persisted beyond what history already records.
3) History window command tab: the History GTK window gains a Commands view (or row expansion) showing command, purpose, exit code, duration, collapsible output, with Copy and Re-run buttons - re-run re-posts through the same confirmation flow as a fresh voice command (never silent).
4) doctor: command-mode line (enabled, tool count, destructive patterns count, context window).

Where: fluidvoice/command.py (schema, classification, context store), fluidvoice/ai/ (prompt changes for multi-tool + context if the prompt lives there - planner verifies), fluidvoice/history.py (no schema change needed - rows already carry fields; any additions must be backward-tolerant reads), fluidvoice/gtkui/main_window.py (Commands view), fluidvoice/config.py (new keys + template comments), fluidvoice/doctor.py, tests/test_command_mode.py (extend: multi-tool parse, destructive classification table incl. user patterns, context window expiry/clear, re-run confirmation), tests/test_gtkui.py (commands view rows/actions).

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; a multi-tool transcript dispatches to the named tool with validated args; a command matching a destructive pattern cannot execute without the strong-confirm step (unit-proven state machine); a follow-up command sees prior output within the window and not after expiry/clear; History Commands view lists, copies, and re-runs through confirmation; upstream parity table updated with file:line citations and deliberate divergences noted in docs/STATUS.md.

Out of scope: autonomous chaining (one voice run = one command set, always confirmed), persistent chat sessions across daemon restarts, agent/tool plugins beyond the schema, AT-SPI, anything that executes without an explicit confirm event.
