Fast language switching + wrong-language hallucination guard (C-track leapfrog; strongest beat-upstream chance per docs/research/2026-09-05-fluidvoice-reviews.md: upstream #506 multilingual switching promised but unshipped since 2026-04; #100 closed with "Parakeet doesn't allow language selection"; EN/DE users get Russian hallucinations; Superwhisper is recommended partly for automatic language switching). SayItErmano already has general.language and per-model overrides model.languages = {model_key: code} (settings depth pack). The gaps: switching requires opening Settings (no runtime action), and whisper-auto can lock onto the wrong language with no guard.

STATUS: SHIPPED

<!-- shipped in 052df8a (series d604db8..052df8a) -->

Scope:
1) Language-cycle hotkey: config hotkey.language_key (keysym, default "" = off) cycles an ordered runtime override through general.language_cycle (new list of language codes, default [], empty = feature off even if key bound). Effective language precedence per take: runtime-cycle override > per-model languages[model_key] > general.language. The cycle override is RUNTIME state (daemon), never persisted to config on its own; daemon status + tray tooltip show the current effective language; a brief overlay/notification (reuse the existing pill/notify fallback) announces e.g. "Language: sl" on switch so it is usable eyes-free. cycle list may include "auto".
2) Wrong-language guard (whisper multilingual backends only; planner verifies which backends accept a language hint): new general.language_whitelist = [] (empty = off). When the effective language is "auto" and whitelist is non-empty: after decode, if the detected/decoded language is not in the whitelist, re-decode once with language = whitelist[0] and log one line (detected=ru, retry=sl); if the backend exposes the detected language only via verbose output and it is unavailable, skip silently and note the limitation in doctor. Parakeet English-only models are unaffected (no language selection - same as upstream's #100 closed rationale; doctor states it).
3) Settings: General page gains the language_cycle editor (ordered chips/list, add/remove) and the whitelist editor (comma-separated codes with validation against the whisper language table already in the codebase - planner verifies where codes are enumerated, e.g. the language picker from the GTK v1.1 work); Hotkey page gains the language_key capture.
4) Doctor: language section - effective language source (cycle/per-model/general), cycle list, whitelist state, per-backend guard applicability.
5) Tests: cycle state machine incl. wrap-around and empty-list-off; precedence table (fake config matrix); whitelist retry path with a fake backend returning lang then corrected text; no-guard for parakeet-class models; doctor lines; config validation (unknown codes rejected using the existing language table).

Where: fluidvoice/config.py, fluidvoice/hotkey.py (language_key grab), fluidvoice/daemon.py (cycle state + effective-language resolution + announce), fluidvoice/backends/ (language hint + detected-language surfacing where supported; planner verifies per backend), fluidvoice/doctor.py, fluidvoice/gtkui/settings_window.py, fluidvoice/tray.py (tooltip), tests/test_language_switch.py (new).

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; live smoke: bind language_key, cycle auto->en->sl->auto with the announcement visible, dictation in Slovenian with general.language=auto+whitelist=[sl,en] returns the sl retry path when the fake-detection scenario is forced; status/doctor report the effective source.

Out of scope: automatic language detection models (Silero/langid), UI language switching (app translation), per-shortcut language (separate), model re-training or fine-tunes, remote endpoints.

Deliverable constraint: the planning phase produces a plan document under specs/ only - planning never edits implementation, config, docs, or test files (the builder phase owns all code changes).
