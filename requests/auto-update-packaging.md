Auto-update + packaging breadth - roadmap "Later" items: "Auto-updater (or packaged releases); onboarding" and "Packaging: AUR, nix, pipx (deb is DONE)". Motivating incident 2026-09-04: the daily-driver machine was upgraded to v0.5.0 by hand-pipping into the relocated user venv while the old 0.2.1 deb kept autostarting from /etc/xdg/autostart - the two-daemon XGrabKey race that silently killed the hotkey for a whole session. A check-and-assist updater prevents exactly this class of drift. GitHub releases exist (v0.4.0, v0.5.0 on acailic/SayItErmano); the Reddit announcement already points at the release install URL.

STATUS: SHIPPED

<!-- shipped in 55c2062 -->

Today: there is no update check anywhere - fluidvoice/ has no update module; the deb (packaging/build-deb.sh) installs to /opt/fluidvoice-linux with a bundled venv, the user install is ~/.local/share/sayit-ermano/venv + ~/.local/bin; `sayit-ermano doctor` reports environment but not version freshness. CI is manual-only by project rule (never add push/PR triggers) - the updater is client-side checking against GitHub releases, not a publishing pipeline.

Scope:
1) Update check module (fluidvoice/update.py): GitHub releases API (api.github.com/repos/acailic/SayItErmano/releases/latest, 10 s timeout, one-shot per daemon start + a daily re-check timer; never blocks startup - runs in a thread); compares semver against __init__ version; result surfaces in status (update_available: version/url), doctor (current vs latest line), and a single desktop notification when a newer release is first seen (ui.notify; aDismissed state in a small JSON beside config; config key updates.check = true default, updates.notify = true).
2) Assisted upgrade, deb path first: "copy install command" action - the notification/status/doctor show the exact terminal command for the install method detected (deb dpkg -i on the downloaded asset URL; user-install: the pip-in-venv one-liner already documented in README - detect install method via paths/markers). NO silent self-installation in v1 - the updater informs, the human runs one command. History window About/status area shows the same line.
3) pipx/PYPI hygiene: verify entry points + data files make `pipx install sayit-ermano` work from the repo (pip install from a built sdist/wheel locally; publishing to PyPI stays manual by the CI rule), README section with the pipx path; fix whatever packaging metadata blocks it.
4) AUR recipe: PKGBUILD maintained in packaging/aur/ (source = release asset or git tag; maintainer note; NOT published by us - instructions only), README links it.
5) Onboarding pass is explicitly out of this request's scope except one thing: the onboarding window gains the "updates on" sentence so new installs know a check happens.

Where: fluidvoice/update.py (new), fluidvoice/daemon.py (thread + status field + notification), fluidvoice/doctor.py, fluidvoice/cli.py (status output + a `sayit-ermano update` subcommand that prints the detected-method command without running it), fluidvoice/gtkui/ (About/status line, onboarding sentence), pyproject.toml (packaging metadata as needed), packaging/aur/PKGBUILD (new), README.md, tests/test_update.py (new: semver compare table, method detection, dismissed-state, offline-mode no-crash).

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; with the network mocked, a newer release produces exactly one notification, status shows update_available, and `sayit-ermano update` prints a correct copy-paste command for BOTH install methods (unit-detected via marker injection); no network / API failure degrades anything (one WARN in doctor at most); pipx install from a locally built wheel launches the daemon --version successfully in a sandboxed check documented in the plan; PKGBUILD lints (namcap or manual review note) with the README section merged.

Out of scope: silent/unattended upgrade, delta updates, beta channel, signature verification beyond asset checksum printing, nix packaging (AUR + pipx only this pass), any CI/publish automation (project rule: manual releases only), rewriting the deb.
