Dictionary auto-learning from corrections - roadmap "Later" item "Custom-dictionary auto-learning from post-insertion corrections"; docs/UPSTREAM-TRACKING.md:168 lists upstream's version as "Auto-suggested custom-dictionary replacements" (⏳). Unblocked now that inline repair exists (history.update_text, aa24ebf): the user's own edits are the signal. Upstream semantics must be verified against altic-dev/Fluid-oss during planning (threshold? suggest vs auto-add? where surfaced?) and quoted file:line in the spec.

STATUS: SHIPPED

<!-- shipped in 55d3f9c -->

Today: update_text (fluidvoice/history.py:154) overwrites entry["text"] in place - the pre-edit text is LOST, so nothing can be learned. The custom dictionary (processing/dictionary.py, config comment shows the canonical case: triggers ["miro board"] -> replacement "Miro board") is purely hand-maintained via the Settings editor.

Scope:
1) Signal capture: update_text keeps the pre-edit text on the entry (entry["edited_from"] = old text when it changes; entries without it simply predate the feature; JSONL readers must tolerate the new key - verify read_all/older readers). No other capture path in v1 (not re-dictation, not external editors).
2) Extraction: new fluidvoice/processing/dict_learn.py - token-level diff (difflib.SequenceMatcher on whitespace tokens) of edited_from -> text; a candidate is one replaced span where BOTH sides are 1-3 tokens of letters (case-insensitive base match counts as a case-suggestion - exactly the "miro board" -> "Miro board" case). Noise exclusions: pure punctuation or numeric spans, tokens in the filler list, both sides identical after case-fold, diffs that rewrite more than 3 tokens (that is editing, not correcting).
3) Suggest, never auto-add: accumulate candidates with counts in a small JSON beside the config (paths module; suggest a name like dictionary-suggestions.json); surface in Settings -> Processing as a Suggestions group above/below the existing dictionary editor: "miro board -> Miro board (seen 3x)" with Accept / Dismiss. Accept adds {triggers: [old], replacement: new} via the existing dictionary merge path without duplicating triggers; Dismiss records the pair so it is never resuggested. Show a suggestion only after >= 2 occurrences (adjust to upstream's threshold if the source says one). If upstream auto-adds silently, diverge deliberately (suggest-only) and note it in docs/STATUS.md - silent dictionary growth degrades trust (research §5 already cited in repo).
4) doctor: one line (pending suggestion count + file path). Config: no new keys unless upstream verification demands a toggle; default behavior on.

Where: fluidvoice/history.py (edited_from), fluidvoice/processing/dict_learn.py (new: extract, load/save suggestions, accept/dismiss helpers), fluidvoice/gtkui/settings_window.py (Suggestions group - post-parakeet tree, plan against then-HEAD), fluidvoice/doctor.py (one line), tests/test_history.py (edited_from kept/absent), tests/test_dict_learn.py (new: extraction table incl. all noise negatives, threshold, dedup vs existing dictionary, accept-merge, dismiss-permanence), tests/test_gtkui.py (rows render, accept posts, dismiss hides), tests/test_infra.py (doctor line).

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; unit tables for the extraction positives/negatives above; accept produces a working dictionary entry (apply_custom_dictionary then rewrites the old form) without duplicate triggers; dismissed pairs stay dismissed across restarts; JSONL round-trips with and without edited_from.

Out of scope: n-best/beam vocabulary boosting, AT-SPI, learning from re-dictation, learning at insertion time, touching apply_custom_dictionary semantics, per-language dictionaries.
