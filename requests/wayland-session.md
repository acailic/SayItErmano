Wayland session support - ROADMAP v0.3 milestone, fully open today: "Insertion via ydotool/wtype + wlr virtual-keyboard protocol", "Hotkey: document/bind DE shortcuts per compositor (GNOME/KDE/COSMIC); optional evdev listener for physical push-to-talk", "Clipboard via wl-clipboard (wl-copy/wl-paste) with restore". docs/UPSTREAM-TRACKING.md marks Wayland insertion as the remaining smart-typing gap. The port is currently X11-only end-to-end (hotkey grabs, overlay, insertion, clipboard); GNOME and KDE default to Wayland and Pop!_OS is mid-transition, so this is the reach multiplier.

STATUS: SHIPPED

<!-- shipped in dbe307f -->

Today: fluidvoice/hotkey.py (XGrabKey), fluidvoice/overlay.py (X11 override-redirect pill), fluidvoice/insertion.py (xdotool type / X11 clipboard paste with the verified-paste design), fluidvoice/selection.py, fluidvoice/tray.py (SNI - already display-server-neutral) all assume X11; daemon start on a Wayland session dies in hotkey setup (HotkeyError "cannot open X display" - verify exact failure during planning; doctor reports it).

Scope (phased; each phase must leave a working X11 regression state - Wayland is additive, never a rewrite of the X11 paths):
1) Session probe + honest startup: detect XDG_SESSION_TYPE/XDG_CURRENT_DESKTOP at daemon start; on wayland, hotkey/overlay/insertion degrade to declared Wayland implementations with a doctor matrix (per-capability: x11 / wayland / unavailable); the daemon must START and stay useful (tray, socket, CLI toggle, file transcribe) on a Wayland session with zero X11 calls failing loudly.
2) Insertion first (highest value): wtype for unicode text; ydotool as fallback (config insertion.wayland_tool = auto|wtype|ydotool, resolved at runtime, tool-not-found degrades to wl-copy+ctrl+v paste); clipboard path via wl-clipboard with the same restore discipline as X11 (snapshot wl-paste, write wl-copy, paste keys per terminal config, restore) - the X11 verified-paste read-observation CANNOT be replicated on Wayland (no selection-read observation across clients), so paste verification degrades to fixed-delay with a doctor note; typed insertion via the wayland tool is the default (no clipboard flash at all), matching how xdotool type works on X11.
3) Hotkey: no global grabs on Wayland - ship the DE-shortcut assist: Settings gains a "Wayland hotkey" page (GNOME: custom-command instructions + a generated script path to bind; KDE: shortcut file import) binding to `sayit-ermano toggle`; doctor prints the per-compositor instructions. Optional evdev push-to-talk listener (config hotkey.wayland_evdev = false default): reads /dev/input by device name (needs uinput/input group - doctor checks permissions; document that this is a privileged path).
4) Overlay: notification-fallback first (the existing replaceable-notification preview path - verify it is display-neutral), layer-shell pill later (wlr-protocol compositors only); on GNOME-Wayland the pill is not possible v1 - the notification preview is the Wayland preview, stated plainly in doctor + README.
5) README "Wayland support" matrix section; comparison docs updated.

Where: fluidvoice/paths.py or a new fluidvoice/session.py (probe), fluidvoice/insertion.py (wayland tool paths), fluidvoice/selection.py (wayland clipboard snapshot/restore), fluidvoice/hotkey.py (X11 listener gated; no wayland grab), fluidvoice/overlay.py/preview fallback (verify neutrality), fluidvoice/daemon.py (capability wiring), fluidvoice/gtkui/settings_window.py (Wayland page), fluidvoice/doctor.py (matrix), fluidvoice/config.py, README.md, tests/test_wayland_capabilities.py (new: probe + tool resolution + insertion command construction with fakes; evdev listener unit with a fake device stream).

Done means: a phased plan under specs/ where each phase leaves `.venv/bin/python -m pytest -q tests --ignore=tests/integration` green; with XDG_SESSION_TYPE=wayland and fake tools on PATH, the daemon starts, status reports the capability matrix, and a transcript inserts via the resolved tool's constructed command line (unit-asserted); doctor on a real Wayland session prints the matrix with per-tool found/missing; the GNOME shortcut page renders the bindable command; on X11 nothing changes (full existing suite green, zero behavior deltas); a live Wayland smoke checklist (sway or GNOME session, manual) is documented in the plan with results noted in docs/STATUS.md.

Out of scope: layer-shell pill UI, portal-based global shortcuts (RemoteDesktop portal) - note as future, screencast/OCR anything, changing X11 code paths beyond the capability gating, compositor-specific patches, CI runners for Wayland.
